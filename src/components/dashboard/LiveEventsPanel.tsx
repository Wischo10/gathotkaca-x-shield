import { Panel } from "@/components/ui/Panel";
import type { SocTelemetry } from "@/types/soc";

export function LiveEventsPanel({ telemetry }: { telemetry?: SocTelemetry }) {
  return <Panel title="Live Events (Last 10)">
    <div className="overflow-x-auto"><table className="w-full min-w-[680px] text-left text-xs"><thead><tr className="border-b border-slate-200 text-[10px] uppercase tracking-wide text-slate-400 dark:border-slate-800"><th className="py-2 pr-3 font-semibold">Time</th><th className="py-2 pr-3 font-semibold">Event</th><th className="py-2 pr-3 font-semibold">Source</th><th className="py-2 pr-3 font-semibold">Severity</th><th className="py-2 pr-3 font-semibold">Rule</th><th className="py-2 font-semibold">Asset / User</th></tr></thead><tbody>{telemetry?.liveEvents.map((event) => <tr key={event.id} className="border-b border-slate-100 dark:border-slate-800"><td className="py-2 pr-3 text-slate-500">{event.time ? new Date(event.time).toLocaleTimeString() : "-"}</td><td className="py-2 pr-3 text-slate-700 dark:text-slate-200">{event.event}</td><td className="py-2 pr-3 text-slate-500">{event.source}</td><td className="py-2 pr-3 capitalize">{event.severity}</td><td className="py-2 pr-3 text-slate-500">{event.rule}</td><td className="py-2 text-slate-500">{event.assetOrUser}</td></tr>)}</tbody></table>{!telemetry && <div className="flex min-h-44 items-center justify-center text-center text-xs text-slate-400">Live Wazuh events unavailable</div>}{telemetry && telemetry.liveEvents.length === 0 && <div className="flex min-h-44 items-center justify-center text-center text-xs text-slate-400">No recent events</div>}</div>
  </Panel>;
}
